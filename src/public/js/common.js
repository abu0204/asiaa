// AOS about us
 AOS.init({
    duration: 1000,   // animation duration in ms
    offset: 10,      // offset (px) from the original trigger point
    once: false        // whether animation should happen only once
  });